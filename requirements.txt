# Core
python-telegram-bot==21.4
requests>=2.31

# Audio: STT (Whisper) + TTS (edge-tts)
faster-whisper>=1.0.3
edge-tts>=6.1.10
av>=11.0.0

# URL / YouTube fetch
trafilatura>=1.12
youtube-transcript-api>=1.0.0
# yt-dlp = caption fallback when YouTube blocks the server IP (see urlfetch.py)
yt-dlp>=2025.6.9
PySocks>=1.7.1

# Health server
aiohttp>=3.9

# Database (multi-user storage)
motor>=3.5
pymongo>=4.8
